CREATE TABLE User_Detail ( id INT AUTO_INCREMENT PRIMARY KEY, user_id INT NOT NULL, education VARCHAR(100), education_level VARCHAR(50), major VARCHAR(100), expected_salary INT, job_objective VARCHAR(100), FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE );